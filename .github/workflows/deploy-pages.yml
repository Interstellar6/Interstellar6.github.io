name: Deploy relumeow.top

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: relumeow-pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout central site
        uses: actions/checkout@v4

      - name: Checkout linked static site (AIIntroduceCourse)
        uses: actions/checkout@v4
        continue-on-error: true
        with:
          repository: Interstellar6/AIIntroduceCourse
          path: linked/AIIntroduceCourse
          fetch-depth: 1

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install build dependencies
        run: python -m pip install pyyaml

      - name: Build static shell
        run: RELUMEOW_PAGES_SHELL_ONLY=1 python build_site.py

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v6

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: _site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
